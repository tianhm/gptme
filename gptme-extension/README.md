# gptme Chrome Extension (old location)

This extension has moved to `webui/extension/`. See [webui/extension/README.md](webui/extension/README.md).

The side panel now uses shared React components via the webui Vite build.
The service worker and content script remain as standalone TypeScript.
